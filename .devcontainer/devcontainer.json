{
	"name": "FEniCS-Dolfinx Production BSSN",
	"image": "dolfinx/dolfinx:stable",
	
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"ms-toolsai.jupyter",
				"ms-toolsai.jupyter-keymap",
				"ms-toolsai.jupyter-renderers",
				"ms-vscode.cpptools"
			],
			"settings": {
				"python.defaultInterpreterPath": "/dolfinx-env/bin/python3",
				"python.linting.enabled": true,
				"python.linting.pylintEnabled": false,
				"python.formatting.provider": "none",
				"terminal.integrated.defaultProfile.linux": "bash"
			}
		}
	},
	
	"features": {
		"ghcr.io/devcontainers/features/git:1": {},
		"ghcr.io/devcontainers/features/github-cli:1": {}
	},
	
	"workspaceFolder": "/workspace",
	"workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind",
	
	"postCreateCommand": "bash -c 'pip install matplotlib numpy scipy astropy spiceypy h5py && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi && echo \"\" && echo \"=== Production BSSN Environment Ready ===\" && echo \"Real ephemeris: Astropy (built-in) ✓\" && echo \"Optional SPICE: Run scripts/install_spice_kernels.sh\" && echo \"Optional Einstein Toolkit: Run scripts/install_einstein_toolkit.sh\" && echo \"\" && echo \"Quick start: cd solar_system_simulations/proper_bssn && python3 solar_system_proper_bssn.py\"'",
	
	"remoteUser": "root",
	
	"mounts": [
		"source=${localWorkspaceFolder}/data,target=/workspace/data,type=bind"
	]
}

